import 'package:flutter/material.dart';

TextStyle buttonFontSize = TextStyle(
  fontSize: 30,
  fontFamily: 'Montserrat'
);

// Theme icon Color or TextColor for dt
Color dtIconColor = Colors.blueAccent;
Color ltIconColor = Colors.grey[850];

// Theme Text Color
Color dtTextColor = Colors.white70;
Color ltTextColor = Colors.grey[850];

